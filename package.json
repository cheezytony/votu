{
  "name": "votu",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter votu-backend start:dev",
    "dev:frontend": "pnpm --filter votu-frontend dev",
    "build:backend": "pnpm --filter votu-backend build",
    "build:frontend": "pnpm --filter votu-frontend build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
